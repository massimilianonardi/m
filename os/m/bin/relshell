#!/bin/sh

#-------------------------------------------------------------------------------

PATH="$(for k in $(echo "$PATH" | tr ':' '\n')
do
  if [ "$k" != "${0%/*}" ]
  then
    echo "$k"
  fi
done | tr '\n' ':')"

export PATH="$SRC_DIR/os/m_rel/bin:${PATH%:}"

bash -i

#-------------------------------------------------------------------------------
