#!/bin/sh

#-------------------------------------------------------------------------------

mk_type_project()
{
# todo keep trac of descending project path
echo todo keep trac of descending project path
  echo INDEPENDENT PROJECT MAKE COMMAND: "$THIS_ABS_PATH" "$PROJECT" $TARGET
  "$THIS_ABS_PATH" "$PROJECT" $TARGET
}

#-------------------------------------------------------------------------------
