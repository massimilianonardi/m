#!/bin/sh

#-------------------------------------------------------------------------------

. env.lib.sh
. term.lib.sh

. array.lib.sh
. menu.lib.sh

#-------------------------------------------------------------------------------

env_defaults()
{
  true
  # set proper initial variables values
}

args_init()
{
  true
  # process args "$@"
}

render()
{
  true
  # only render inside region
}

key_default()
{
  true
  # handle keys not explicitly mapped
}

key_q()
{
  return 1
  # exits from key loop
}

key_esc()
{
  return 1
  # exits from key loop
}

key_enter()
{
  # if dir then browse new dir, else do nothing
  if [ -d "$file" ]
  then
    menu_dir "$file"
  fi
}

env_list_get()
{
  true
  # echo "myvar1 myvar2 myvar3"
  # env_list "term_TEMPLATE_\|custom_var_pattern"
}

#-------------------------------------------------------------------------------

. tui-main.src.sh
