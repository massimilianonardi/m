#!/bin/sh

#-------------------------------------------------------------------------------

. map.lib.sh

#-------------------------------------------------------------------------------

test_map()
{
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -t x1
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -b

  # encoded="$(
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -b | tr -d '\t\r\n'
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -t x1 | tr -d '\t\r\n ' | xxd -r -p
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -t x1 | tr -d '\t\r\n ' | sed -e 's/^\(.\(.\{2\}\)*\)$/0\1/' -e 's/\(.\{2\}\)/\\x\1/g'
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -t x1 | tr -d '\t\r\n ' | sed -e 's/^\(.\(.\{2\}\)*\)$/0\1/' -e 's/\(.\{2\}\)/ 0x\1/g'
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -t x1 | tr -d '\t\r\n '
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -t x1 | tr -d '\t\r\n ' | sed -e 's/^\(.\(.\{2\}\)*\)$/0\1/' -e 's/\(.\{2\}\)/ 0x\1/g' | xargs -I % printf "%d\n" %
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -t x1 | tr -d '\t\r\n ' | sed -e 's/^\(.\(.\{2\}\)*\)$/0\1/' -e 's/\(.\{2\}\)/ 0x\1/g' | xargs -I % printf "%d\n" %
  # printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -t x1 | tr -d '\t\r\n ' | od -An -t uC
# )"

  # for k in $(printf "Test Encoding/Decoding - # @ $ _ ? ' §" | od -A n -b | tr -d '\t\r\n')
  # do
  #   # echo "$k"
  #   printf "\\$k"
  # done

  # echo "encoded=$encoded"
  # printf "%d\n" "$encoded"
  # printf "%d\n" 0xFF

  # a2o "Test Encoding/Decoding - # @ $ _ ? ' §"
  # o2a "$(a2o "Test Encoding/Decoding - # @ $ _ ? ' §")"

  # exit 0

  # mymap_test="testval"
  map mymap
  # map mymap
  # map mymap "my first key" "second arg" "3rd arg" "4 arg" "5 arg" "6 arg"
  map mymap size
  map mymap size my_map_size; echo "my_map_size=$my_map_size"
  map mymap put "my_first_key" "first key value"
  map mymap put "my wrong key" "wrong key value"
  # map mymap get "my_first_key"
  # map mymap get "my_first_key" myvar; echo "myvar=$myvar"
  # map mymap get "my wrong key"
  # map mymap get "my wrong key" myvar; echo "myvar=$myvar"
  map mymap keys
  map mymap get
  map mymap size
  # set | grep mymap
  # map mymap set "new val 0" "new val 1" "new val 2" "new val 3" "new val 4"
  # map mymap set "new 'val 0'" "new 'val \'1'" "new val 2" "new val 3" "new val 4"
  # saveargs "new 'val 0'" "new 'val \'1'" "new val 2" "new val 3" "new val 4"
  # map mymap get
  # set | grep mymap
  map mymap rem "my wrong key"
  map mymap keys
  set | grep mymap
  # map mymap get
  # eval set -- $(map mymap get); for k in "$@"; do echo "$k"; done
  # for k in eval echo "$(map mymap get)"; do echo "$k"; done
  map mymap unset
  echo "map unset"
  set | grep mymap
}

#-------------------------------------------------------------------------------
