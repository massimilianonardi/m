#!/bin/sh

test_rsudo()
{
  export LOG_LEVEL=all
  # log_level all

  export RSUDO_HOST="localhost"
  export RSUDO_USER="$USER"

  # export RSUDO_PASSWORD="qwe123"
  # export RSUDO_PASSWORD="$(readpass "enter rsudo test password:")"
  # unset RSUDO_PASSWORD
  # export  RSUDO_INTERACTIVE="true"

  # rsudo ######################################################################
  # # ask for password if not available in env
  # rsudo ls -ld

  # # password exported
  # RSUDO_PASSWORD="AAAAAAAAA" echo "$RSUDO_PASSWORD" | rsudo ls -ld ### error because does not read pass from pipe (RSUDO_ASKPASS not set)
  # export RSUDO_PASSWORD="AAAAAAAAA"; echo "$RSUDO_PASSWORD" | RSUDO_ASKPASS="true" rsudo ls -ld

  # # commands from pipe
  # echo "ls -lA" | rsudo

  # # no command from arg or pipe => start an interactive ssh session into sudo su bash
  # rsudo
  # rsudo ""

  # commands from args interactive session
  # RSUDO_INTERACTIVE="true" rsudo sh -c "read -p 'type something: ' x; echo 'you typed: '\$x"
  # echo "pwd" | RSUDO_INTERACTIVE="true" rsudo sh -c "read -p 'type something: ' x; echo 'you typed: '\$x"
  # RSUDO_INTERACTIVE="true" rsudo top
  # rsudo top # error because executed not interactively

  # command from arg wich receives input from pipe when not interactive and when interactive execute pipe commands, then args
  # echo "message from pipe" | rsudo cat
  # echo "ls -la; x='test_val'; echo \"x evaluated from pipe=\$x\"" | RSUDO_INTERACTIVE="true" rsudo echo "x=\$x"

  # command from heredoc are streamed through pipe to ssh, thus input from pipe is discarded (by posix sh design)
#   echo "message from pipe" | rsudo << EOF
# # for above reasons eventual password could only be streamed inside heredoc (not proper way to do)
# pwd
# cat # not printing anything for the reasons above
# EOF

  # correct way to combine stdin commands with commands from heredoc (heredoc and stdin commands cannot access stdin)
#   (echo 'echo "ls -lA"'; echo "cat"; echo 'var="test value"'; cat << EOF; echo "echo after heredoc") | rsudo
# pwd; cat
# echo "var=\$var"
# echo "end of cat"
# EOF

  # this version uses strict heredoc (no vars interpreted) and set vars from cmd line then available inside heredoc
#   (echo 'echo "ls -lA"'; echo "cat"; echo 'var="test value"'; cat << 'EOF'; echo "echo after heredoc") | rsudo
# pwd; cat
# echo "var=$var"
# echo "end of cat"
# EOF

  # wrong way to provide password because RSUDO_PASSWORD or is already be defined, or is null
  # and combine stdin commands with commands from heredoc
#   (echo "$RSUDO_PASSWORD"; echo 'echo "ls -lA"'; cat << EOF) | rsudo
# pwd; cat
# echo "end of cat"
# EOF

  # must work
  # rsudo
  # rsudo top # error because executed not interactively
  # RSUDO_INTERACTIVE="true" rsudo top
  # export LOG_LEVEL="none"
  # rsudo sh -c "ls -ld"
  # rsudo sh -c 'ls -ld'
  # rsudo sh -c "ls -ld; whoami; whoami && pwd"
  # rsudo "ls -ld; whoami; whoami && pwd"
  # rsudo 'ls -ld; whoami; whoami && pwd'
  # rsudo 'whoami; sh -c "ls -ld"'
  # rsudo "whoami; sh -c 'ls -ld'"
  # RSUDO_INTERACTIVE="true" rsudo "sh -c \"read -p 'type something: ' x; echo 'you typed: '\\\$x; whoami\"" # double quotes implies nested escaping
  # RSUDO_INTERACTIVE="true" rsudo 'sh -c "read -p \"type something: \" x; echo \"you typed: \"\$x; whoami"' # single quotes simplify escaping madness
  # RSUDO_INTERACTIVE="true" rsudo sh -c "read -p \"type something: \" x; echo \"you typed: \$x\"; whoami" # complex command instead of one string cmd
  # RSUDO_INTERACTIVE="true" rsudo "read -p 'type something: ' x; echo 'you typed: '\$x; whoami"
  # RSUDO_INTERACTIVE="true" rsudo 'read -p "type something: " x; echo "you typed: $x"; whoami'
  # rsudo sh -c "x='my x value'; echo 'your x='\$x; whoami"
  # rsudo sh -c 'x="my x value"; echo "your x=$x"; whoami'
  # rsudo 'x="my x value"; echo "your x=$x"; whoami'
  # echo "message from pipe" | rsudo cat
  # rsudo -- 'ls -ld; whoami; whoami && pwd'
  # rsudo --user admino -- 'ls -ld; whoami; whoami && pwd'
  # rsudo --user mnardi -- 'ls -ld; whoami; whoami && pwd'
  # rssh
  # rsudo --interactive 'read -p "type something: " x; echo "you typed: $x"; whoami'
  # read x; echo "$x" | rsudo --askpass --interactive
  # rsudo --connect admino@localhost "ls -ld; whoami; whoami && pwd"
  # rsudo --connect mnardi@localhost "ls -ld; whoami; whoami && pwd"
  # . rsudo-env.lib.sh
  # rsudoenv set "myhost"
  # echo "step_1"; env | grep "RSUDO" | sort
  # rsudoenv save "/tmp/rsudoenv_vars.enc"
  # rsudoenv edit "/tmp/rsudoenv_vars.enc"
  # rsudo --load "/tmp/rsudoenv_vars.enc":myhost "ls -ld; whoami; whoami && pwd"
  # rm -f "/tmp/rsudoenv_vars.enc"

  # read x; echo "$x" | RSUDO_ASKPASS="true" RSUDO_INTERACTIVE="true" rsudo
  # echo "pwd;" | RSUDO_INTERACTIVE="true" rsudo sh -c "read -p 'type something: ' x; echo 'you typed: '\$x; whoami"
  # echo "pwd" | RSUDO_INTERACTIVE="true" rsudo "read -p 'type something: ' x; echo 'you typed: '\$x; whoami; su"
  # echo "pwd" | RSUDO_INTERACTIVE="true" rsudo "sh -c \"read -p 'type something: ' x; echo 'you typed: '\\\$x; whoami\""
  # echo "ls -la; x='test_val'; echo \"x evaluated from pipe=\$x\"" | RSUDO_INTERACTIVE="true" rsudo echo "x=\$x"
  # (echo "whoami;"; echo "x='test_val'; echo \"x evaluated from pipe=\$x\"") | RSUDO_INTERACTIVE="true" rsudo echo "x=\$x"

#   (cat << EOF_vars; cat << 'EOF_script') | rsudo
# var_1="multiple heredocs: 1"
# var_2="multiple heredocs: 2"
# EOF_vars
# echo "var_1=$var_1"
# echo "var_2=$var_2"
# EOF_script

  # rsudo-env.lib.sh ###########################################################
  # . rsudo-env.lib.sh

  # set myhost from current
  # rsudoenv set "myhost"
  # echo "step_1"; set | grep "RSUDO" | sort
  # # copy myhost into myhost_2
  # RSUDO_HOST="fake_host" RSUDO_USER="fake_user" RSUDO_PASSWORD="fake_pass" rsudoenv set "myhost_2" "myhost"
  # echo "step_2"; set | grep "RSUDO" | sort
  # # delete myhost_2
  # rsudoenv unset "myhost_2"
  # echo "step_3"; set | grep "RSUDO" | sort
  # # delete current
  # rsudoenv unset
  # echo "step_4"; set | grep "RSUDO" | sort
  # # set current from myhost
  # rsudoenv get "myhost"
  # echo "step_5"; set | grep "RSUDO" | sort
  # # save and edit test
  # rsudoenv pass "store pass for enc/dec next files"
  # rsudoenv set "myhost_2" "myhost"
  # rsudoenv save "/tmp/rsudoenv_vars.enc"
  # rsudoenv edit "/tmp/rsudoenv_vars.enc"
  # rsudoenv editor "gnome-text-editor"
  # rsudoenv edit "/tmp/rsudoenv_vars.enc"
  # rsudoenv unset
  # rsudoenv unset "myhost"
  # echo "step_6"; set | grep "RSUDO" | sort
  # rsudoenv get "/tmp/rsudoenv_vars.enc:myhost"
  # echo "step_7"; set | grep "RSUDO" | sort
  # rsudoenv unset
  # rsudoenv unset "myhost"
  # rsudoenv get "/tmp/rsudoenv_vars.enc:myhost"
  # echo "step_8"; set | grep "RSUDO" | sort
  # rsudoenv unset
  # rsudoenv get ":myhost"
  # echo "step_9"; set | grep "RSUDO" | sort
  # rm -f "/tmp/rsudoenv_vars.enc"
  # rsudoenv get "myuser@myhost"
  # echo "step_10"; set | grep "RSUDO" | sort
  # rsudoenv get "@myhost_2"
  # echo "step_11"; set | grep "RSUDO" | sort
  # rsudoenv exec "myhost myhost_2" rsudo "ls -ld"

  # rsudo export ###############################################################
  # rsudo export LOG_LEVEL echo "inside rsudo session: LOG_LEVEL=\$LOG_LEVEL"
  # rsudo export LOG_LEVEL echo 'inside rsudo session: LOG_LEVEL=$LOG_LEVEL'
  # echo "echo \"inside rsudo session: LOG_LEVEL=\$LOG_LEVEL\"" | rsudo export LOG_LEVEL
  # echo 'echo "inside rsudo session: LOG_LEVEL=$LOG_LEVEL"' | rsudo export LOG_LEVEL
  # echo 'echo "inside rsudo session: LOG_LEVEL=$LOG_LEVEL"' | rsudo # without export var is null
  # echo 'echo "inside rsudo session: LOG_LEVEL=$LOG_LEVEL"' | rsudo export LOG_LEVEL echo 'inside rsudo session 2: LOG_LEVEL=$LOG_LEVEL'

  # TEST_EXPORT_VAR_1="test_val_1"; TEST_EXPORT_VAR_2="test_val_2"; TEST_EXPORT_VAR_3="test_val_3";
  # rsudo-export TEST_EXPORT_VAR_1 TEST_EXPORT_VAR_2 TEST_EXPORT_VAR_3 -- echo "inside rsudo session: TEST_EXPORT_VAR_1=$TEST_EXPORT_VAR_1 TEST_EXPORT_VAR_2=$TEST_EXPORT_VAR_2 TEST_EXPORT_VAR_3=$TEST_EXPORT_VAR_3"

  # rsudo fs ###################################################################
  # test_files_start
  # read x

  # # NOT working because globbing is NOT interpreted server side (quotes are preserved)
  # rsudo fs delete -v "${TEST_DIR}/*"
  # # working because globbing is interpreted server side using --no-preserve-quotes flag
  # rsudo --no-preserve-quotes fs delete -v "${TEST_DIR}/*"
  # rsudo fs delete -v "${TEST_DIR}/000"

  # # not working because multiple paths inside one argument
  # rsudo fs delete "'${TEST_DIR}/000/'* '${TEST_DIR}/aaa'/*"

  # # globbing server side + multiple entries (with eventual spaces or special characters)
  # rsudo fs delete "\"${TEST_DIR}/000\"/*" "${TEST_DIR}/aaa/*"

  # rsudo fs put "${TEST_DIR}/aaa/link-recursive" "${TEST_DIR}/upload/link-aaa-recursive"
  # rsudo fs put "${TEST_DIR}/aaa/bbb/ccc/link-absolute" "${TEST_DIR}/upload/link-absolute"
  # rsudo fs put "${TEST_DIR}/aaa/file_a1" "${TEST_DIR}/upload/file_a1"
  # rsudo fs put "${TEST_DIR}/aaa/colliding/sub-colliding/file_colliding" "${TEST_DIR}/upload/file_colliding_renamed"
  # rsudo fs put "${TEST_DIR}/000" "${TEST_DIR}/upload/000" "admino:admino" "777"
  # rsudo fs put "${TEST_DIR}/000" "${TEST_DIR}/upload/0-0-0"
  #
  # rsudo fs get "${TEST_DIR}/aaa/link-recursive" "${TEST_DIR}/download/link-aaa-recursive"
  # rsudo fs get "${TEST_DIR}/aaa/bbb/ccc/link-absolute" "${TEST_DIR}/download/link-absolute"
  # rsudo fs get "${TEST_DIR}/aaa/file_a1" "${TEST_DIR}/download/file_a1"
  # rsudo fs get "${TEST_DIR}/aaa/colliding/sub-colliding/file_colliding" "${TEST_DIR}/download/file_colliding_renamed"
  # rsudo fs get "${TEST_DIR}/000" "${TEST_DIR}/download/000"
  # rsudo fs get "${TEST_DIR}/000" "${TEST_DIR}/download/0-0-0"

  # ls -lA "${TEST_DIR}/upload"
  # ls -lA "${TEST_DIR}/download"
  # read x
  # test_files_end
  # rsudo fs delete "${TEST_DIR}"

  # rsudo db-pg ################################################################
  # # rsudo db-pg psql "$@" # args are psql commands, eg.: multiple -c command, -f file.
  # SQL_QUERY="select * from pg_catalog.pg_tables where tablespace = 'pg_global';"
  # SQL_QUERY="select * from pg_catalog.pg_tables limit 5;"
  # rsudo db-pg psql postgres -c "$SQL_QUERY"
  # rsudo db-pg psql postgres -c '\z' -c "$SQL_QUERY"
  # rsudo db-pg resetdb "AAAAAAAAA"
  # rsudo db-pg putdb "system" "/m/tmp/m/data/webgis/postgres-bak/pg_db_bak_system.sql"

  # rsudo apisix ###############################################################
  # rsudo apisix
  # rsudo apisix test
  rssh << 'EOF'
RSUDO_ENC_PASS="$RSUDO_PASSWORD"
# PATH="/m/src/git/rpr/cmd/rpr-jobs:$PATH"
PATH="/m/src/git/rpr/cmd/rpr-rsudo-jobs:$PATH"
rsudoenv_load rpr-env.enc
# set | grep RSUDO_ENV

# rsudoenv get portale
# rsudo --interactive
rsudoenv_get apisix
# rsudoenv get portale_dev
# rsudo apisix key
# rsudo apisix keyset APISIX_ADMIN_KEY
# set | grep APISIX
rsudo apisix json_string_or_array "uri" "uris" "uri1"
rsudo apisix json_string_or_array "uri" "uris" "uri1 uri2"
# env_eval "$APISIX_PLUGIN_CONF_KEYCLOAK_AUTHN_TEMPLATE" "my_client_id" "my_client_secret" "my_callback_uri1"
# env_eval_set "APISIX_TEST_VAR" "$APISIX_PLUGIN_CONF_KEYCLOAK_AUTHN_TEMPLATE" "my_client_id" "my_client_secret" "my_callback_uri1"
# echo "APISIX_TEST_VAR=<<<$APISIX_TEST_VAR>>>"
# rsudo apisix set_auth_vars "apisix_host.local" "443" "keycloak_host.local" "443" "keycloak-realm" "kc_apisix_client_id" "kc_apisix_client_secret" "kc_apisix_client_session_secret"
# rsudo apisix set_auth_vars "apisix_host.local" "9443" "keycloak_host.local" "8443" "keycloak-realm" "kc_apisix_client_id" "kc_apisix_client_secret" "kc_apisix_client_session_secret"
# set | grep APISIX
# KEYCLOAK_USER_NAME="admin"
# KEYCLOAK_USER_PASS="admin"
# KEYCLOAK_HOST="keycloak.rpr-spa.it"
# KEYCLOAK_PORT="8443"
# KEYCLOAK_REALM="master"
# ACCESS_TOKEN="$(curl "https://${KEYCLOAK_HOST}:${KEYCLOAK_PORT}/realms/${KEYCLOAK_REALM}/protocol/openid-connect/token" \
#   -d "client_id=admin-cli" \
#   -d "username=${KEYCLOAK_USER_NAME}" \
#   -d "password=${KEYCLOAK_USER_PASS}" \
#   -d "grant_type=password" \
#   -d "scope=openid email profile" \
#   | sed -e 's/^.*access_token":"//g' -e 's/",".*//g')"
# echo "ACCESS_TOKEN=$ACCESS_TOKEN"
# curl -i "https://keycloak.rpr-spa.it:8443/admin/realms/rpr/clients" -X GET \
#   -H "Authorization: Bearer ${ACCESS_TOKEN}" \
#   -d "scope=openid email profile" \
#   | sed -e 's/"id":/\n\n"id":/g'
# curl -i "https://keycloak.rpr-spa.it:8443/admin/realms/rpr/clients/0e7de9cc-81d2-44ac-8d20-fdb51c1df9c2/client-secret" -X GET \
#   -H "Authorization: Bearer ${ACCESS_TOKEN}" \
#   -d "scope=openid email profile"
# curl -i "https://keycloak.rpr-spa.it:8443/admin/realms/rpr/clients/0e7de9cc-81d2-44ac-8d20-fdb51c1df9c2" -X GET \
#   -H "Authorization: Bearer ${ACCESS_TOKEN}" \
#   -d "scope=openid email profile"
# curl -i "https://keycloak.rpr-spa.it:8443/admin/realms/rpr/users" -X GET \
#   -H "Authorization: Bearer ${ACCESS_TOKEN}" \
#   -d "scope=openid email profile" \
#   | sed -e 's/"id":/\n\n"id":/g'
# curl "https://${KEYCLOAK_HOST}:${KEYCLOAK_PORT}/realms/${KEYCLOAK_REALM}/protocol/openid-connect/userinfo" \
#   -H "Authorization: Bearer ${ACCESS_TOKEN}" \
#   -d "scope=openid email profile"
# curl -i "https://keycloak.rpr-spa.it:8443/admin/realms/master/users/admin" -X GET \
#   -H "Authorization: Bearer ${ACCESS_TOKEN}" \
#   -d "scope=openid email profile"
rsudo keycloak set_auth_vars "keycloak.rpr-spa.it" "8443" "master"
# rsudo keycloak access_token admin admin
rsudo keycloak access_token_set ACCESS_TOKEN admin admin
echo "ACCESS_TOKEN=$ACCESS_TOKEN"
# rsudo keycloak set_auth_vars "keycloak.rpr-spa.it" "8443" "rpr"
# set | grep KEYCLOAK
# rsudo apisix api "routes"
# rsudo apisix get "routes"
# rsudo apisix create service_simple "test-service-id" "https" "my_host.local:80"
# rsudo apisix get "services"
# rsudo apisix create route "test-route-id" "uri1 uri2" '"service_id": "test-service-id"'
# rsudo apisix get "routes"
# rsudo apisix delete route "test-route-id"
# rsudo apisix get "routes"
# rsudo apisix delete service "test-service-id"
# rsudo apisix get "services"
EOF

  # rsudo keycloak #############################################################
  # rsudo keycloak

  # rssh #######################################################################
  # rssh
  # # executed and shell stays open
  # rssh eval 'export PATH="/m/src/git/rpr/cmd/rpr-jobs:$PATH"; rsudoenv_load rpr-env.enc rpr-env-pg.enc; set | grep RSUDO_ENV'
#   # executed and shell exits
#   rssh << 'EOF'
# export PATH="/m/src/git/rpr/cmd/rpr-jobs:$PATH"; rsudoenv_load rpr-env.enc rpr-env-pg.enc; set | grep RSUDO_ENV
# set | grep rsudo
# EOF
  # echo '#!/bin/sh\nexport PATH="/m/src/git/rpr/cmd/rpr-jobs:$PATH"; rsudoenv_load rpr-env.enc rpr-env-pg.enc; set | grep RSUDO_ENV; echo "args: $@"' > "/tmp/rssh_tmp_script"
  # chmod +x "/tmp/rssh_tmp_script"
  # # executed as sourced and correct args inside sourced script, outside args are full, shell stays open
  # rssh . "/tmp/rssh_tmp_script" arg1 arg2
  # rm -f "/tmp/rssh_tmp_script"

#-------------------------------------------------------------------------------
}
