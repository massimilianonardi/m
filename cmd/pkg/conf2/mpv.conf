#!/bin/sh

LATEST_RELEASE_URL="https://deb-multimedia.org/pool/main/m/mpv-dmo/"

VERSION=$(wget --no-check-certificate -qO- "$LATEST_RELEASE_URL" | grep "href" | sed 's/.*href="//g' | sed 's/".*//g' | head -n 1)
# DOWNLOAD_URL="https://www.xnview.com/download.php?file=XnViewMP-linux-x64.tgz"
DOWNLOAD_URL="${LATEST_RELEASE_URL}${VERSION}"

FILE_TYPE="tgz"
SYMLINKS="mpv=mpv"
