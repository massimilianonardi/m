
--------------------------------------------------------------------------------
m install review m dirs and test m install from scratch online (with certs support)
m install and pkg have /m/app and /m/bin hardcoded, pkg has also pkg/conf/home -> parametrize all m paths
log lib + command
pkg install support app launchers env vars (one for all alunchers) specific command line options for each launcher
pkg install implement move and link conf from home (with support of files not yet created!)
pkg uninstall eventually data home + postgres
pkg conf review: chromium sandbox, firefox, gson, madedit-mod
find best file manager PCManFM-Qt, make madedit work copy/paste

rsudo file manager
use env lib tricks like for term export and make a trick to provide env by cat heredoc envs + cat heredoc script that allows all
log, log lib, rsudojobs, rsudo check envs in command line sub-calls and in /proc/$pid/environ

!!! keycloak userinfo + roles
keycloak change totp provider key
keycloak backup restore + disaster recovery + recreate by rest api calls by means of helper functions

gis: allow apisix, deny all
apisix installer
apisix rsudo scripts for admin automation (also keycloak???!!! maybe keycloak can run remotely api rest calls)

-> keycloak auth page rpr style
-> (NOT NEEDED ANYMORE) geoserver keycloak auth
-> geoserver no auth -> web admin > Authentication > Filter Chains > disable security for "web" filter (and any other)
-> geoserver behind proxy and apisix custom path -> PROXY_BASE_URL + GEOSERVER_CSRF_WHITELIST + jetty-web.xml contextPath + apache2 + apisix coherent mappings (all absolute)
-> geoserver paths/routes into apisix for admin authorization and specific workspace/layer authorization
-> snapshot/bak 26
-> renew 26 with new data structure, but old app and auth (try to develop a final rpr-mnode-full?)
-> think about portale, upgrade: condono, ai infante, giubileo, municipio7, tevere, webgis, cpq_2025, osp, vas, vvss (servizi) -> layers conf + layout, searches
-> portale: webgis/bestgis, cpq/cpq, webgiscpq/???, webgisvas/vas@2024, regionelazio/webgis, municipio7/municipio7, condono/abuso, osp/osp, servizi/servizi, tevere/tevere

app.rpr-spa.it upgrade: patrimonio 4785, patrimonio scan, simu scan
git bare repositories rework
gitlab install

gis edit:
- QGIS/ArcGIS direct access to db or filesystem
- QGIS/ArcGIS access by geoserver wfs-t direct access to filesystem
- db versioning + filesystem versioning

gis improve:
- production env: https://docs.geoserver.org/stable/en/user/production/index.html
- geoserver cloud: https://geoserver.org/geoserver-cloud/
- geOrchestra: https://www.georchestra.org/software.html
- GeoNetwork: https://geonetwork-opensource.org/
- GeoFence: https://github.com/geoserver/geofence
- GeoWebCache: https://geowebcache.osgeo.org/
- MapStore: https://docs.mapstore.geosolutionsgroup.com/en/latest/
- GeoNode: https://geonode.org/
- GeoPrisma: https://geoprisma.mapgears.com/site/index.php
- CloudBeaver: https://github.com/dbeaver/cloudbeaver/
- ByteBase: https://www.bytebase.com/
- SnowFlake: https://www.snowflake.com/it/
- Terraform: https://developer.hashicorp.com/terraform
- LinuxContainers: https://linuxcontainers.org/
--------------------------------------------------------------------------------
