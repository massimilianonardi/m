
- auth / keycloak: kc + postgres
- apigw / apisix: apisix + etcd
- micro / srv / srvs / apps / apps-dev: microservices -> html, js, resources + java-db + java-file-manager + geoserver
apache, php, postgres, java, geoserver, wildfly

microservices + postgres bound on 127.0.0.1 (also gis micro dedicated server), openresty/nginx expose https and allows only apisix

integrate keycloak: geoserver, gitlab, headless cms
rest api to create resources and permissions on keycloak, thus it is possible to use sh functions to create dynamically apisix routes and keycloak resources/permissions
every postgres instance for each user (keycloak, geoserver, mapserver, java-service, etc.)

production servers:
auth-keycloak -> keycloak (dmz -> intranet domain controller)
apisix auth-proxy -> apisix (dmz -> intranet microservices)
auth -> apps multiple servers + microservices (intranet) -> gis (postgres, raster files, mapserver, multiple geoserver), db (postgres, java-db), file (gluster?), war (wildfly), src (gitlab)

apisix on port 443 an remove any http socket, decoupled deployment, protect admin port access, protect dashboard allowing access only from selected intranet ips, coraza plugin
https://apisix.apache.org/docs/apisix/deployment-modes/

keycloak on port 443, decouple admin rest from login flows, put behind reverse proxy to achieve that?

Roundcube Webmail keycloak

production: [ web | webapps | intranet ].rpr-spa.it only url-cloaker or proxy

--------------------------------------------------------------------------------

http://apisix.rpr-spa.it:9180/ui/
https://keycloak.rpr-spa.it:8443
https://apps.rpr-spa.it/keycloak

https://apps.rpr-spa.it/keycloak/realms/master/hostname-debug

https://apps.rpr-spa.it/apisix/authz/ip
https://apps.rpr-spa.it/apisix/authz/headers

--------------------------------------------------------------------------------

/usr/local/openresty
/usr/local/openresty/nginx/
/usr/local/openresty/nginx/conf/nginx.conf

openresty -t && systemctl restart openresty
systemctl status openresty

less /usr/local/openresty/nginx/logs/access.log
less /usr/local/openresty/nginx/logs/error.log

--------------------------------------------------------------------------------

less /usr/local/apisix/logs/access.log
less /usr/local/apisix/logs/error.log

--------------------------------------------------------------------------------

/m/pkg/keycloak-26.4.2/keycloak-26.4.2/conf/keycloak.conf
systemctl restart keycloak
systemctl status keycloak
less /m/pkg/keycloak-26.4.2/keycloak-26.4.2/data/log/keycloak.log
bin/kc.sh --spi-theme-static-max-age=-1 --spi-theme-cache-themes=false --spi-theme-cache-templates=false
rm-f keycloak/data/tmp

--------------------------------------------------------------------------------

NB accesses are managed by global firewall

apps: the only server with public access, proxy to restricted servers apisix and keycloak to hide admin paths and expose public-authorizated services.
  serves a default page with iframe to hide direct urls. putting everything under this proxy prevents cors errors
apisix: restricted access to admin users and "apps" proxy server, exposes authenticated/authorizated services
keycloak: restricted access to admin users and "apps" proxy server, exposes api to authenticate/authorize public users
services (exposed by apisix): restricted access to apisix and developers.
  static + simple content server by same server with apisix, bound to 127.0.0.1, one gis server (mapserver, geoserver, postgres, extra (allegati, schede, metadati, ecc.))
  apps on apisix, content on separate servers.
gis server: /mapserver, /geoserver, /allegati, /schede, /metadati, /mini-metadati, ecc.

apps is public, services allows apisix by ip/hostname through nginx, apisix allows only apps, keycloak proxied by apps nginx???

---

apps nginx allows only apisix and every app service is bound to 127.0.0.1 (also postgres db) -> if needed this node should be replicated and sychronized for load balancing
apps should not host data, a dedicated postgres cluster and a dedicated webgis server for all legacy data and new remapped
apisix is the public point 9443 with apache2/nginx/openresty 443 url-cloak page
keycloak is public, or proxied by apisix or apps
webgis-data: metadati, mini-metadati, schede, allegati, db, mapserver, geoserver -> apache2 + php, mnode-standalone -> links to /m/data/webgis/links to autoupdate on src pull
/m/data/webgis/raster + allegati
webgis-src+db+files: dev, test, prod

--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
